# First local article

This is the body of the first local article. The migration script should
strip the leading H1 title and persist the *raw* markdown into ``cards.body``
while caching the rendered HTML in ``cards.body_html``.

## A subsection

```python
def hello() -> str:
    return "world"
```

- a list item
- another item
